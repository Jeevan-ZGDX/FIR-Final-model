# Project requirements / environment manifest
# NOTE: This project is a Node.js (React + Vite) application. This file documents
# the Node engine requirement and the npm dependencies/devDependencies with
# versions for easy reference. This is not a Python requirements.txt; use the
# commands below to install Node packages.

# Environment
node>=20.19.0
npm>=10.8.0

# Runtime dependencies (npm packages)
axios@1.11.0
ethers@6.15.0
jspdf@3.0.1
react@19.1.1
react-dom@19.1.1
react-router-dom@7.8.2
styled-components@6.1.19
web3modal@1.9.12

# Dev dependencies (npm packages)
@eslint/js@9.33.0
@types/react@19.1.10
@types/react-dom@19.1.7
@vitejs/plugin-react@5.0.0
eslint@9.33.0
eslint-plugin-react-hooks@5.2.0
eslint-plugin-react-refresh@0.4.20
globals@16.3.0
vite@7.1.2

# Optional runtime dependency added for OCR (lazy-loaded in the browser)
# (install only if you plan to use the OCR/camera feature in FIRForm.jsx)
tesseract.js

# How to use
# 1) Install or switch Node to a supported version (recommended via nvm-windows):
#    nvm install 20.19.0
#    nvm use 20.19.0
#
# 2) From project root (PowerShell):
#    cd 'D:\clg notez\BlockChain COE projects\FIR log system project\Final model De-FIR\proto-model-build-frontend-fir\proto-model-build-frontend-fir'
#    npm ci            # preferred for CI (installs exact versions from package-lock.json)
#    # or
#    npm install       # installs per package.json (may update package-lock.json)
#
# 3) Start dev server:
#    npm run dev
#
# 4) If you added OCR support, install tesseract.js (one-time):
#    npm install tesseract.js
#
# Notes:
# - If you encounter engine errors, ensure your active Node matches the 'node'>=20.19.0 requirement above.
# - Add a .nvmrc or .node-version file if you want to pin Node for contributors.
# - This file is intended as an environment manifest; package installation and
#   actual dependency management should be done via package.json / npm.
